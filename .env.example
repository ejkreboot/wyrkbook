PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

ANTHROPIC_API_KEY=

# Absolute origin used when generating assignment QR codes (no trailing slash).
PUBLIC_SITE_URL=http://localhost:5173

# Domain for the synthetic addresses student accounts hang off. Students sign in
# with a username and never receive mail, so this must not be a real domain.
STUDENT_EMAIL_DOMAIN=students.invalid
